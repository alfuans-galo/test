<!DOCTYPE html>
<html lang="en">
    <head>
      <!-- Required meta tags always come first -->
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
      <meta http-equiv="x-ua-compatible" content="ie=edge">

      <title>RETO</title>

      <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
      <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
      <%= csrf_meta_tags %>

    </head>
    <body>


      <div class="container">
        <nav class="navbar navbar-expand-sm bg-light">
          <!-- Brand/logo -->
          <%= link_to image_tag('logo.png', :width=>"35"), root_path, class: 'navbar-brand' %> 
          
          <!-- Links -->
          <ul class="nav nav-pills" role="tablist">
            <% if current_user %>
              <li class="nav-item">
                <%= link_to 'edit profile', edit_user_registration_path(current_user), class:'btn btn-lg btn-outline-primary', role:"button" %>
              </li>
              <li class="nav-item">
                <%= link_to 'albums', albums_path, class:'btn btn-lg btn-outline-primary', role:"button" %>
              </li>
              <li class="nav-item">
                <%= link_to 'logout', eval("destroy_#{current_user.type.downcase}_session_path"), method: :delete, class:'btn btn-lg btn-outline-primary' %>

              </li>
            <% end %>
          </ul>
        </nav>
              
        <div class="container-fluid">
          <%= render partial: 'shared/flash' unless flash.empty? %>
          <%= yield %>
        </div>
      </div>


    </body>
</html>

